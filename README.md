# advent-of-code-2025

Source of the solutions are in the [src/bin](src/bin) directory and are coded in Rust.

From the project main directory, run each solution like this:
```
cargo run --bin 01-secret-entrance
```